## LangChain Expression Language
LangChain 声明式构建「链式」调用流程的方式，允许开发者使用 .pipe() 将不同的模块链接起来，形成一个完整的链。

- 只要是 Runnable 的模块，那就可以使用 .pipe() 链接；

## 结构（形成链条，可以做插拔）
提示词  -> 模型 -> 解析器

## 核心方法 
- invoke: 同步的调用；
- pipe: 通用的组合模块方法，将输出接入到下一个模块，主要用来拼接；
- stream: 获取流式输出；
- batch: 同时处理多个输入，返回一个结果数组；

## 并发请求
